interface Check_photo {
        abstract var yesPhotoCount: Int
        abstract var noPhotoCount: Int
        abstract var checkCount: Int
        abstract var totalDiscountAmount: Int
        fun showCheck_photo()
}